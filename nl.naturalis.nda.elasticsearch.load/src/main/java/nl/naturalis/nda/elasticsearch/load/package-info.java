/**
 * Generic classes for the NBA import. This packages provides a set
 * of interfaces and abstract classes that together constitute the
 * ETL (Extract, Transform, Load) framework for the NBA import. These
 * interfaces and abstract classes are implemented c.q. extended by
 * all datasource-specific import libraries except the NSR import
 * library, which still needs to be converted to the new framework.
 */
package nl.naturalis.nda.elasticsearch.load;
